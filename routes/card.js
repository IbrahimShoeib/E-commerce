const mongoose = reqire("mongoose")
const express = require("express")
const product = require("../../E=commerce/routes/product")
router=express.Router()



router.post("/",async(req,res) => {
    //
})